#include <stdio.h>
#include <stdlib.h>

int main()
{
	int num;
	char name[100];
	FILE *file;
	file = fopen("Desired address","a");
	if(file == NULL)
	{
		printf("\nERROR!!");
	}
	printf("\nEnter the character: ");
	scanf("%s %d", &name , &num);

	fprintf(file,"%s %d", name , num);
	fclose(file);
	return 0;
}